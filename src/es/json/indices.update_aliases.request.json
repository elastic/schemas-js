{"$schema":"https://json-schema.org/draft/2020-12/schema","title":"indices.update_aliases","type":"object","properties":{"master_timeout":{"$ref":"./_types.json#/$defs/_types__Duration","description":"Period to wait for a connection to the master node.\nIf no response is received before the timeout expires, the request fails and returns an error."},"timeout":{"$ref":"./_types.json#/$defs/_types__Duration","description":"Period to wait for a response.\nIf no response is received before the timeout expires, the request fails and returns an error."},"actions":{"type":"array","items":{"$ref":"./indices.update_aliases.json#/$defs/indices.update_aliases__Action"},"description":"Actions to perform."}},"description":"Create or update an alias.\n\nAdds a data stream or index to an alias."}