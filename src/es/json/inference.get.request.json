{"$schema":"https://json-schema.org/draft/2020-12/schema","title":"inference.get","type":"object","properties":{"task_type":{"$ref":"./inference._types.json#/$defs/inference._types__TaskType","description":"The task type of the endpoint to return"},"inference_id":{"type":"string","description":"The inference Id of the endpoint to return. Using `_all` or `*` will return all endpoints with the specified\n`task_type` if one is specified, or all endpoints for all task types if no `task_type` is specified"}},"description":"Get an inference endpoint.\n\nThis API requires the `monitor_inference` cluster privilege (the built-in `inference_admin` and `inference_user` roles grant this privilege)."}