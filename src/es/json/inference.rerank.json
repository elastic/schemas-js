{"$schema":"https://json-schema.org/draft/2020-12/schema","$defs":{"inference.rerank__RerankQuery":{"oneOf":[{"type":"string"},{"$ref":"./inference.rerank.json#/$defs/inference.rerank__RerankInputObject"}],"description":"Query input for the `rerank` task.\nEither a string, or an object. The object form additionally allows specifying non-text inputs, such as images.\n\n> info\n> Only the `elastic` service currently supports non-text queries for the `rerank` task."},"inference.rerank__RerankInputObject":{"type":"object","properties":{"type":{"$ref":"./inference.rerank.json#/$defs/inference.rerank__RerankInputType","description":"The type of input. Not all services and models support all input types."},"format":{"$ref":"./inference.rerank.json#/$defs/inference.rerank__RerankInputFormat","description":"The format of the input. For the `text` type this must be `text`. For the `image` type this must be `base64`.\nIf not specified, this defaults to `text` for the `text` type and `base64` for the `image` type."},"value":{"type":"string","description":"The value of the input. For images, this must be a base64-encoded data URI, that is, \"data:content/type;base64,...\"."}},"required":["type","value"],"description":"An object describing a single input for the `rerank` task, which additionally allows specifying non-text inputs, such as images."},"inference.rerank__RerankInputType":{"enum":["text","image"]},"inference.rerank__RerankInputFormat":{"enum":["text","base64"]},"inference.rerank__RerankInput":{"oneOf":[{"$ref":"./inference.rerank.json#/$defs/inference.rerank__RerankStringInput"},{"$ref":"./inference.rerank.json#/$defs/inference.rerank__RerankObjectInput"}],"description":"The documents to rank for the `rerank` task.\nEither a string, an array of strings, an object, or an array of objects.\nThe object form additionally allows specifying non-text inputs, such as images.\n\n> info\n> Only the `elastic` service currently supports non-text inputs for the `rerank` task."},"inference.rerank__RerankStringInput":{"oneOf":[{"type":"string"},{"type":"array","items":{"type":"string"}}],"description":"Allows specifying text-only documents to rank for the `rerank` task."},"inference.rerank__RerankObjectInput":{"oneOf":[{"$ref":"./inference.rerank.json#/$defs/inference.rerank__RerankInputObject"},{"type":"array","items":{"$ref":"./inference.rerank.json#/$defs/inference.rerank__RerankInputObject"}}],"description":"Allows specifying documents to rank as objects, which additionally supports non-text inputs, such as images."}}}