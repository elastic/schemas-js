{"$schema":"https://json-schema.org/draft/2020-12/schema","title":"inference.delete","type":"object","properties":{"dry_run":{"type":"boolean","description":"When true, checks the semantic_text fields and inference processors that reference the endpoint and returns them in a list, but does not delete the endpoint.","x-found-in":"query"},"force":{"type":"boolean","description":"When true, the inference endpoint is forcefully deleted even if it is still being used by ingest processors or semantic text fields.","x-found-in":"query"},"task_type":{"$ref":"./inference._types.json#/$defs/inference._types__TaskType","description":"The task type","x-found-in":"path"},"inference_id":{"type":"string","description":"The inference identifier.","x-found-in":"path"}},"required":["inference_id"],"description":"Delete an inference endpoint.\n\nThis API requires the manage_inference cluster privilege (the built-in `inference_admin` role grants this privilege)."}